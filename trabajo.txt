// Instalación de Angular
npm install -g @angular/cli
// Creación de proyecto
ng new nombre-del-proyecto
// Iniciar proyecto
npm start ó ng serve
// Generación de componentes
ng generate component usuario    -      ng g c usuario
// Versionamiento en GIT
git init
git remote add origin URL-del-repositorio
git add .
git commit -m "Proyecto Angular inicial"
git push -u origin main

// Proyecto de Clase
1. npm i bootstrap@5.3.7
2. Angular.json/styles "node_modules/bootstrap/dist/css/bootstrap.min.css"
3. ng g c components/navbar
4. ng g c components/agregarEditarComentario
5. ng g c components/listComentarios 
6. app.html add listComentarios and integrate with ts file in imports.
7. ng g i interfaces/comentario 
8. ng g s services/comentario
9. add navbar of bootstrap in the component navbar.
10. add code for listComentarios.html and add container in app.componets for main app
11. add button "Nuevo comentario" in listComentarios.
12. add table in listComentarios with class table table-hover mt-3, with 5 columns
13. add var listComentarios in componet ts listComentarios of type comentario interface.
14. use *ngFor=let comentario of  and user interpolation {{  }}
15. add conditions @if (listComentarios.length === 0)
16. Add routes in the app.routes.ts and edit app.cofig.ts
17. Add routerLink in button add new comment in listComentarios.
18. Import RouterLink in ts for listComentarios.
19. Add HTML for Comentario.
Semana 8
21. Agregar ReactiveFormsModule en app.cofig.ts 
22. Agregar codigo de formulario en el constructor de agregarEditarComentario.
23. Bindear el form group con el formulario html.
24. Agrega ReactiveFormsModule al import de agregarEditarComentario para poder Bindear.
25. Agregar el (ngSubmit) y el tipo submit al boton así como la funcion agregar.
26. Crear un objeto de tipo comentario y setear los valores del formulario.
27. Agregar mensajes de validación en los campos de los formularios.
Semana 9
20. Agregar el routerLink en el vinculo del logotipo del navbar.
28. Redireccionar de listComentarios a verComentarios (Por vinculo)
29. Agrega html y codigo de verComentario.
30. Crear proyecto Asp.Net Core Api en Visual Studio Community  (https://localhost:7162/openapi/v1.json)
31. Agregar Scalar.AspNetCore para el API y configurar Program.cs y launchSettings.json agregando Scalar (https://localhost:44309/scalar/)
32. Agregar controlador ComentarioController
33. Instalar Entity Framework Core, Entity Framework Tools, Entity Framework Core Sql Server
34. Agregar Carpeta Models y Clase (Modelo) Comentario.cs
35. Agregar clase AplicationDbContext.cs y configurar el DbSet y el constructor.
36. Agregar la cadena de conexión en appsettings.json
36. Agregar la configuración de la DB en Program.cs y generar la migración Add-Migration v1.0.0
37. Migrar a la DB: Update-database
---------------------------------------------------------------------------------------------
Parte del despliegue del proyecto.

Dockerización del Proyectos:
1. Installar Docker en su computadora.
2. Dockerizar el proyecto actual de angular.
    a. Crear un archivo docker-compose.yml, (Crear una cuenta en docker hub.)
    b. Crear presentación
    c. Explicar a detalle el funcionamiento del archivo docker-compose.yml
    d. Levantar el proyecto con docker.
VPS:
3. VPS (Linux -> Ubuntu) y acceder al VPS  por SSH.

Integración de GitHub Actios.
1. Instalación de docker en el VPS.
2. Generar un clave publica y privada para el VPS.
3. Generar un workflows: .github/workflows/deploy.yml
4. Progar el pipeline creado.